spring:
  application:
    name: aimud
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/aimud
    username: aimud
    password: aimud_password
    pool:
      initial-size: 5
      max-size: 20
  docker:
    compose:
      enabled: true
      file: docker-compose.yml
